# Web framework for the API and MVP dashboard.
Flask>=3.0,<4.0

# Postgres driver and connection pooling.
psycopg[binary,pool]>=3.2,<4.0

# Kubernetes API client used by the deployment worker/reconciler.
kubernetes>=29.0,<30.0

# HTTP client for proxying inference requests to vLLM.
requests>=2.32,<3.0

# Local development .env loading.
python-dotenv>=1.0,<2.0

# Password hashing.
argon2-cffi>=23.1,<24.0

# User access token encoding.
PyJWT>=2.8,<3.0

# Test runner.
pytest>=8.0,<9.0

# Python linter.
ruff>=0.6,<1.0
