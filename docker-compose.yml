services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: miniten
      POSTGRES_USER: miniten
      POSTGRES_PASSWORD: miniten
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
